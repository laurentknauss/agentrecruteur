# Backend environment (production, when DEMO_LOCK is removed)
# DEMO: leave OPENAI_API_KEY undefined and set DEMO_LOCK=1 via systemd unit

# OpenAI
OPENAI_API_KEY=
GPT_MODEL=gpt-5.5

# MongoDB (optional, enables persistent store)
MONGODB_ATLAS_URI=
MONGODB_DATABASE=cv-inspector
